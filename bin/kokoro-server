#!/bin/bash
# Talkback Local — compatibility shim; the code lives in the talkback package. Removed next release. (No argument meant start.)
exec "${TALKBACK_PYTHON:-python3}" -m talkback server "${@:-start}"
